import React from 'react';
import { NextPage } from 'next';
import ReduxSampleContainer from 'modules/ReduxSample/container';

const Page: NextPage = () => {
  return <ReduxSampleContainer />;
};

export default Page;
